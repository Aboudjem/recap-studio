# Multi-editor setup has moved

This page is now [`docs/editors.md`](editors.md). One file covers both ways in: installing the
skills with `npx skills add`, and adding the local MCP server to Claude Code, Cursor, VS Code,
Codex, Gemini CLI, Windsurf, Continue, OpenCode and Zed.

The old page also pointed at `npx @recap-studio/cli`, which does not resolve: the packages are not
published to npm. The new page uses the paths that work from a built clone.
